import 'package:flutter/material.dart';

class SingleChatPageMobile extends StatelessWidget {
  const SingleChatPageMobile({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}