import 'package:flutter/material.dart';

class SingleChatPageTablet extends StatelessWidget {
  const SingleChatPageTablet({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}